# Las rutas de usuarios se registran en el router central (config/urls.py).
# Este archivo queda como referencia si el equipo prefiere separar las rutas
# de autenticación en su propio router más adelante.
